# 🛰 SkyLink: Инструкция по эксплуатации и сборке

**SkyLink** — это клиентский модуль для передачи телеметрии на портал SkyBioML. Приложение обеспечивает сбор данных в реальном времени и их безопасную отправку через API.

---

## 🛠 1. Подготовка окружения

Перед любыми манипуляциями убедись, что среда настроена корректно.

1.  **Активация виртуального окружения:**
    * *Windows:* `.venv\Scripts\activate`
    * *Linux/macOS:* `source .venv/bin/activate`
2.  **Установка зависимостей:**
    ```bash
    pip install -r requirements.txt
    ```

---

## 🧪 2. Тестовый режим и отладка

Для проверки функционала без создания финального билда используй прямой запуск через интерпретатор.

### Команды запуска
* **Стандартный запуск:**
    ```bash
    python gui.py
    ```
* **Режим расширенного логирования (если реализован `logging`):**
    ```bash
    python gui.py --debug
    ```

### Чек-лист проверок (Manual Tests)
Прежде чем считать версию стабильной, проверь эти точки отказа:
- [ ] **Сеть:** Приложение не вылетает при отсутствии интернета.
- [ ] **Авторизация:** Корректная обработка неверного API-ключа портала.
- [ ] **Валидация данных:** Попытка отправки "битой" или пустой телеметрии блокируется GUI.
- [ ] **Логи:** В папке проекта создается/обновляется файл логов.

---

## 📦 3. Режим сборки (Build Mode)

Когда код стабилизирован, приложение упаковывается в исполняемый файл для конечного пользователя.

### Команда компиляции (PyInstaller)
Используем `PyInstaller` для создания одного `.exe` файла (Windows) без лишних окон консоли:

```bash
pyinstaller --noconfirm --onefile --windowed --name "SkyLink" --clean gui.py

