🚀 SkyLink Agent: Инструкция по сборке и релизу
Этот документ описывает полный цикл обновления приложения: от кода до релиза на GitHub.

📦 1. Подготовка к новой версии
Перед началом сборки нужно обновить номер версии в файлах.

В файле build.py:

Найди строку: '--name=SkyLinkV0.8' (поменяй цифру на актуальную).

В файле setup.iss:

#define MyAppVersion "0.8" (Версия для свойств файла).

#define MyAppExeName "SkyLinkV0.8.exe" (Должно совпадать с именем в build.py).

OutputBaseFilename=SkyLink_Setup_v0.8 (Имя готового инсталлятора).

🛠 2. Сборка EXE (PyInstaller)
Мы используем режим Folder (onedir), чтобы антивирусы меньше ругались.

Открой терминал в VS Code.

Удали старые папки build и dist (чтобы не было мусора):

PowerShell

Remove-Item -Recurse -Force build, dist
Запусти скрипт сборки:

Bash

python build.py
Результат: В папке dist должна появиться папка SkyLinkV0.8 с кучей файлов внутри.

💿 3. Создание Инсталлятора (Inno Setup)
Превращаем папку с программой в один красивый setup.exe.

Запусти программу Inno Setup Compiler.

Открой файл setup.iss из папки проекта.

Нажми кнопку Run (▶️ или F9).

Результат: В корне проекта появится файл SkyLink_Setup_v0.8.exe.

⚠️ Если Касперский удалил файл:

Зайди в Касперский -> Безопасность -> Отчеты/Карантин -> Восстановить файл.

Добавь папку проекта в Исключения, чтобы он не мешал работать.

🐙 4. Загрузка кода на GitHub
Отправляем исходный код в облако.

В терминале VS Code:

Bash

git add .
git commit -m "Release v0.8: Fixed bugs, added log rotation"
git push
🌍 5. Публикация Релиза (Для пользователей)
Делаем так, чтобы пилоты могли скачать файл.

Открой репозиторий: https://github.com/III-TAO-III/SkyLink

Справа в блоке Releases нажми Draft a new release.

Choose a tag: Создай новый тег (например, v0.8).

Release title: SkyLink Agent v0.8.

Description: Опиши, что нового (например: Fixed Dr.Tellur login issue, improved logs).

Attach binaries: Перетащи в нижнее поле файл SkyLink_Setup_v0.8.exe.

Нажми зеленую кнопку Publish release.

🎉 Готово! Можно кидать ссылку в Дискорд.

🆘 Если что-то пошло не так
Антивирус ругается у друга: Скажи ему нажать "Подробнее -> Выполнить в любом случае" (SmartScreen) или добавить в исключения. Это норма для софта без платной цифровой подписи ($400/год).

Логи раздулись: В версии v0.8+ включена авто-ротация (макс 5 МБ), файл сам очистится.

Гит ругается на email: Сними галочку "Block command line pushes" в настройках GitHub -> Emails.